diff -ruN oracle-java.orig/Makefile oracle-java/Makefile
--- oracle-java.orig/Makefile	1970-01-01 02:00:00.000000000 +0200
+++ oracle-java/Makefile	2013-04-25 12:38:55.019409714 +0300
@@ -0,0 +1,11 @@
+BINDIR = opt/java
+DISTFILE = jdk-7u21-linux-i586.tar.gz
+
+all:
+	mkdir -p $(BINDIR)
+	(cd $(BINDIR) && tar -zxf ../../$(DISTFILE))
+
+install:
+	cp -pR opt $(DESTDIR)/
+
+clean:
