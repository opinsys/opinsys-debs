description "puavo-ca-rails"

start on runlevel [23]
stop on shutdown

chdir /var/app/puavo-ca-rails

env RAILS_ENV=production

exec sudo -E -u puavoca bundle exec unicorn_rails -c ./config/unicorn.rb

respawn
